// Allow active local users to update SDDM background without a password
polkit.addRule(function(action, subject) {
    if (action.id == "org.illogicalimpulse.sddm-bg" &&
        subject.isInGroup("users") && subject.active) {
        return polkit.Result.YES;
    }
});
